import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.notesorganizer',
  appName: 'Notes Organizer',
  webDir: 'dist'
};

export default config;
